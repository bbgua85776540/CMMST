import os
import numpy as np
import pickle
import header
import argparse

import head_orientation_lib
import saldat_head_orientation


if __name__ == "__main__":

	for topic in range(9):  # 0 1 2 3 4 5
		if topic > 4: continue
		for user in range(36):
			pass
